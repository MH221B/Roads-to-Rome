PORT=3000
ACCESS_TOKEN_SECRET=REPLACE_WITH_SECURE_ACCESS_TOKEN_SECRET
REFRESH_TOKEN_SECRET=REPLACE_WITH_SECURE_REFRESH_TOKEN_SECRET
SESSION_SECRET=REPLACE_WITH_SECURE_SESSION_SECRET
GITHUB_CLIENT_ID=REPLACE_WITH_GITHUB_CLIENT_ID
GITHUB_CLIENT_SECRET=REPLACE_WITH_GITHUB_CLIENT_SECRET
NODE_ENV=development
MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/<database>
CLIENT_URL=http://localhost:5173
RESET_PASSWORD_SECRET=REPLACE_WITH_SECURE_RESET_PASSWORD_SECRET
CALLBACK_URL=http://localhost:3000/auth/github/callback

# Notes:
# 1. Do NOT commit your .env file with real secrets. Add real values to a local .env file instead.
# 2. Use environment variables in production (e.g., via your cloud provider's secret store or Docker secrets).
